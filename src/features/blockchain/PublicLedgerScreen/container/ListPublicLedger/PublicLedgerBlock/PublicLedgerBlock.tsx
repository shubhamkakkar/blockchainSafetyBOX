import React from 'react';
import { View } from 'react-native';

type Props = {
  item: any;
  userId: string;
};

export default function PublicLedgerBlock(props: Props) {
  return (
    <View />
  );
}
