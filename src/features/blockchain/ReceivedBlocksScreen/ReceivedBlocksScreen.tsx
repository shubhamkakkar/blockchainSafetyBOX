import React from 'react';
import MainContainer from 'UI/MainContainer';
import Header from 'UI/Header/Header';

type Props = {

};
export default function ReceivedBlocksScreen(props: Props) {
  return (
    <MainContainer>
      <Header title="Received Blocks" />
    </MainContainer>
  );
}
