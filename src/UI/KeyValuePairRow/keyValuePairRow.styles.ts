import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginBottom: 10,
  },
  keyContainer: {
    marginRight: 5,
  },
  valueContainer: {
    marginLeft: 5,
  },
  flex: {
    flex: 1,
  },
});
